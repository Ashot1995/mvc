<?php

/**
 * Used to define the routes in the system.
 * A route should be defined with a key matching the URL and an
 * controller@action-to-call method
 */
$routes = array(
	'/' => 'figure@index',
	'/result' => 'figure@result'
);
